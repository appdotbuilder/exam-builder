import { type UpdateExamInput, type Exam } from '../schema';

export async function updateExam(input: UpdateExamInput): Promise<Exam | null> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is updating an existing exam's title and/or description.
    // It should update the updated_at timestamp and return the updated exam.
    // Returns null if exam is not found.
    return Promise.resolve(null);
}