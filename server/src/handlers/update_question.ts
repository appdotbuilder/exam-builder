import { type UpdateQuestionInput, type Question } from '../schema';

export async function updateQuestion(input: UpdateQuestionInput): Promise<Question | null> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is updating an existing question's properties.
    // It can update question text, points, or order index independently.
    // Returns null if question is not found.
    return Promise.resolve(null);
}